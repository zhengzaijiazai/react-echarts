import React from "react";
import Title from "@/components/globalTitle";

const Echarts = () => {
  return (
    <div className="index">
      <Title title="echarts" />
    </div>
  );
};

export default Echarts;
